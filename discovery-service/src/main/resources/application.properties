spring.application.name=discovery-service
server.port=8761
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Kubernetes liveness/readiness probes (unauthenticated - no security config
# in this service at all, so no permitAll rule is needed).
management.endpoints.web.exposure.include=health
management.endpoint.health.probes.enabled=true

# Graceful shutdown: finish in-flight requests before the JVM exits -
# Kubernetes sends SIGTERM on pod termination/rolling deploy.
server.shutdown=graceful
spring.lifecycle.timeout-per-shutdown-phase=20s
