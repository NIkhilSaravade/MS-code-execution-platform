"""Phase 3 golden dataset: 5 small LeetCode-style problems, each with a
correct reference solution and one hand-authored mutation injecting a known
bug class. Scope note (logged honestly in docs/ai-agent-build-log.md too):
these mutations are hand-authored per problem, one per bug class, not
generated by a general-purpose AST mutation-testing tool (e.g. mutmut) -
that's a real, smaller scope than "a mutation-testing harness" might imply
in the abstract, chosen because building a generic Python AST mutator was a
bigger undertaking than this phase's budget, and a hand-authored bug is
just as valid a "known, injected bug" for measuring catch rate.

Each entry:
  id                 - short slug
  problem_description
  correct_code        - real, correct Python solution
  mutation            - dict: bug_class, description, mutated_code,
                         correctness_affected (bool - if False, the mutated
                         code still produces correct output; the "bug" is a
                         pure code-quality issue like a resource leak, so
                         it's graded through the PASSED prompt/codeSmells
                         field instead of FAILED/failureReason), and
                         catch_keywords (lowercased substrings - if any
                         appears in the relevant output field, the review
                         is counted as having caught the injected bug)
"""

GOLDEN_DATASET = [
    {
        "id": "two_sum",
        "problem_description": (
            "Given an array of integers nums and an integer target, return the "
            "indices of the two numbers that add up to target. Each input has "
            "exactly one solution, and the same element may not be used twice."
        ),
        "correct_code": (
            "def two_sum(nums, target):\n"
            "    seen = {}\n"
            "    for i, num in enumerate(nums):\n"
            "        complement = target - num\n"
            "        if complement in seen:\n"
            "            return [seen[complement], i]\n"
            "        seen[num] = i\n"
            "    return []\n"
        ),
        "mutation": {
            "bug_class": "comparator_flip",
            "description": "Inverted membership check ('not in' instead of 'in') means a match is never returned.",
            "mutated_code": (
                "def two_sum(nums, target):\n"
                "    seen = {}\n"
                "    for i, num in enumerate(nums):\n"
                "        complement = target - num\n"
                "        if complement not in seen:\n"
                "            return [seen[complement], i]\n"
                "        seen[num] = i\n"
                "    return []\n"
            ),
            "correctness_affected": True,
            "catch_keywords": ["not in", "invert", "logic", "condition", "wrong", "complement"],
        },
    },
    {
        "id": "valid_parentheses",
        "problem_description": (
            "Given a string containing just the characters '(', ')', '{', '}', "
            "'[' and ']', determine if the input string is valid: every opening "
            "bracket must be closed by the same type of bracket, in the correct order."
        ),
        "correct_code": (
            "def is_valid(s):\n"
            "    pairs = {')': '(', '}': '{', ']': '['}\n"
            "    stack = []\n"
            "    for ch in s:\n"
            "        if ch in pairs:\n"
            "            if not stack or stack.pop() != pairs[ch]:\n"
            "                return False\n"
            "        else:\n"
            "            stack.append(ch)\n"
            "    return not stack\n"
        ),
        "mutation": {
            "bug_class": "missing_none_check",
            "description": "Removed the 'if not stack' guard before popping, so a lone closing bracket raises IndexError on an empty stack instead of returning False.",
            "mutated_code": (
                "def is_valid(s):\n"
                "    pairs = {')': '(', '}': '{', ']': '['}\n"
                "    stack = []\n"
                "    for ch in s:\n"
                "        if ch in pairs:\n"
                "            if stack.pop() != pairs[ch]:\n"
                "                return False\n"
                "        else:\n"
                "            stack.append(ch)\n"
                "    return not stack\n"
            ),
            "correctness_affected": True,
            "catch_keywords": ["empty", "indexerror", "pop", "guard", "stack", "check"],
        },
    },
    {
        "id": "max_subarray",
        "problem_description": (
            "Given an integer array nums, find the contiguous subarray with the "
            "largest sum and return its sum (Kadane's algorithm)."
        ),
        "correct_code": (
            "def max_subarray(nums):\n"
            "    best = nums[0]\n"
            "    current = nums[0]\n"
            "    for num in nums[1:]:\n"
            "        current = max(num, current + num)\n"
            "        best = max(best, current)\n"
            "    return best\n"
        ),
        "mutation": {
            "bug_class": "off_by_one",
            "description": "Iterates over the full array (including index 0 again) instead of nums[1:], double-counting the first element into 'current'.",
            "mutated_code": (
                "def max_subarray(nums):\n"
                "    best = nums[0]\n"
                "    current = nums[0]\n"
                "    for num in nums:\n"
                "        current = max(num, current + num)\n"
                "        best = max(best, current)\n"
                "    return best\n"
            ),
            "correctness_affected": True,
            "catch_keywords": ["off-by-one", "off by one", "double", "index 0", "first element", "nums[1:]", "boundary"],
        },
    },
    {
        "id": "is_palindrome",
        "problem_description": (
            "Given a string s, return True if it reads the same forward and backward, ignoring case."
        ),
        "correct_code": (
            "def is_palindrome(s):\n"
            "    normalized = s.lower()\n"
            "    return normalized == normalized[::-1]\n"
        ),
        "mutation": {
            "bug_class": "resource_leak",
            "description": "Opens a debug log file every call and never closes it - correctness is unaffected, but it's a real resource-leak code smell.",
            "mutated_code": (
                "def is_palindrome(s):\n"
                "    debug_log = open('palindrome_debug.log', 'w')\n"
                "    debug_log.write(s)\n"
                "    normalized = s.lower()\n"
                "    return normalized == normalized[::-1]\n"
            ),
            "correctness_affected": False,
            "catch_keywords": ["close", "leak", "file", "open(", "with open", "resource"],
        },
    },
    {
        "id": "fibonacci_memo",
        "problem_description": (
            "Given n, return the nth Fibonacci number using memoization for efficiency."
        ),
        "correct_code": (
            "def fib(n, memo=None):\n"
            "    if memo is None:\n"
            "        memo = {}\n"
            "    if n <= 1:\n"
            "        return n\n"
            "    if n not in memo:\n"
            "        memo[n] = fib(n - 1, memo) + fib(n - 2, memo)\n"
            "    return memo[n]\n"
        ),
        "mutation": {
            "bug_class": "mutable_default_argument",
            "description": "memo={} as a default argument is created once at def time and shared/mutated across every call that doesn't pass its own memo - the classic mutable-default-argument anti-pattern (see knowledge/anti_patterns.md).",
            "mutated_code": (
                "def fib(n, memo={}):\n"
                "    if n <= 1:\n"
                "        return n\n"
                "    if n not in memo:\n"
                "        memo[n] = fib(n - 1, memo) + fib(n - 2, memo)\n"
                "    return memo[n]\n"
            ),
            "correctness_affected": False,
            "catch_keywords": ["mutable default", "shared", "default argument", "memo={}", "persists across calls", "default value"],
        },
    },
]
