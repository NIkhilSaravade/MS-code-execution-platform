-- Matches com.MS_code_execution_service.execution_result_service.entity.ExecutionResult exactly.
CREATE TABLE execution_result (
    id             bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    submission_id  bigint,
    problem_id     bigint,
    user_id        bigint,
    output         TEXT,
    status         varchar(255),
    execution_time bigint
);
