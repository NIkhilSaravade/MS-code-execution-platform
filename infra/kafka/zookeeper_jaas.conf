// Server-side counterpart to the "Client" section in
// infra/kafka/kafka_server_jaas.conf - dev-only fixed credentials, see that
// file's comment for why this exists and its rotation status.
Server {
   org.apache.zookeeper.server.auth.DigestLoginModule required
   user_kafka="zk-dev-secret";
};
